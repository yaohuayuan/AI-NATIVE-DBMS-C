# AI_MATCH Operator

`AI_MATCH` is the first planned LLM semantic operator.

v0.1 does not implement `AI_MATCH`. The future implementation should connect the operator to planning, execution, explain output, caching, call logging, and optimizer decisions.
