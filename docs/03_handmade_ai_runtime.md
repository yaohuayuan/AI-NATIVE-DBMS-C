# Handmade AI Runtime

The handmade AI runtime is planned for later milestones.

v0.1 does not implement model calls, batching, retries, metrics, budgets, or real API access. Those pieces will be introduced after the DBMS baseline and deterministic mock model path are in place.
