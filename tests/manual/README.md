# Manual Tests

Manual tests that call real APIs may live here in later milestones.

This directory is not part of default CTest. Default tests must not require network access, API keys, or real API calls.
